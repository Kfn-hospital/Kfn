# نبذة كاملة عن المشروع — بوابة خورفكان الإدارية

## 1. السياق العام
نظام إداري داخلي لمستشفى خورفكان، منقول من نظام قديم (Google Apps Script + Google Sheets كقاعدة بيانات) إلى تقنيات حديثة، بسبب قيود ومشاكل تقنية كتيرة في النظام القديم (حجب الشبكة لجوجل درايف في شبكة المستشفى، قيود الإطارات المتداخلة sandbox، بطء عام).

## 2. التقنيات المستخدمة (Stack)
- **الواجهة الأمامية والخلفية**: Next.js 15 (App Router)، TypeScript، React 19
- **قاعدة البيانات**: Supabase (PostgreSQL) — بديل Google Sheets
- **المصادقة**: Supabase Auth (بريد إلكتروني + كلمة سر)
- **تخزين الملفات**: Supabase Storage (بديل Google Drive)
- **الاستضافة**: Vercel — نشر تلقائي عند كل `git push` لفرع `main`
- **التنسيق**: Tailwind CSS
- **مستودع الكود**: GitHub — `github.com/Kfn-hospital/Kfn`

## 3. الحالة الحالية — كل ده مبني، مفحوص محليًا (`npm run build` ناجح 100%)، ومرفوع فعليًا

### نظام المصادقة والحماية
- `src/app/login/page.tsx` — صفحة دخول بإيميل/كلمة سر
- `src/middleware.ts` + `src/lib/supabase/middleware.ts` — يحمي كل الصفحات تلقائيًا، أي مستخدم غير مسجّل دخول يتحوّل لصفحة `/login` فورًا
- أول حساب أدمن اتضاف يدويًا عبر: إضافة مستخدم من `Supabase → Authentication → Users`، ثم إدخال صف مطابق في جدول `profiles` بنفس الـ UID، بدور `admin`

### دعم اللغتين (عربي RTL / إنجليزي LTR) — كامل
- `src/lib/i18n/translations.ts` — قاموس مركزي لكل نصوص الواجهة (المفتاح نفسه بالعربي والإنجليزي)
- `src/lib/i18n/LanguageContext.tsx` — Context يحفظ اللغة المختارة في `localStorage`، ويحدّث `document.documentElement.lang/dir` تلقائيًا عند التبديل
- زر تبديل اللغة موجود في القائمة الجانبية وصفحة الدخول

### الثيم البصري (متسق في كل الصفحات)
- شريط جانبي: خلفية `bg-teal-900`، روابط نشطة بخلفية بيضاء
- بطاقات: `bg-white rounded-2xl shadow`
- أزرار أساسية: `bg-teal-700 text-white rounded-xl`
- مكوّنات UI موحّدة في `src/components/ui/`: `Card.tsx`, `DataTable.tsx` (جدول عام قابل لإعادة الاستخدام)، `Modal.tsx`, `FormField.tsx`, `Alert.tsx` (رسائل نجاح/خطأ)

### الصفحات الست — كلها بوظائف CRUD حقيقية متصلة بـ Supabase
| الصفحة | المسار | الوظائف |
|---|---|---|
| القاعات والحجوزات | `src/app/(protected)/dashboard/page.tsx` | عرض القاعات (ثنائية اللغة name/name_en)، إضافة حجز، اعتماد/رفض (أدمن أو مسؤول قاعة)، إلغاء (صاحب الحجز) |
| طلبات التنسيق والمتابعة | `src/app/(protected)/requests/page.tsx` | إضافة طلب، تصنيف (category)، إسناد لموظف، تغيير الحالة مباشرة من قائمة منسدلة |
| قوائم التحقق | `src/app/(protected)/checklists/page.tsx` | إنشاء قائمة ببنود متعددة، تعيين أكتر من شخص لكل بند (كل شخص يأكد بنفسه)، صلاحيات وصول لكل شخص على القائمة كلها (مشاهدة فقط / تعديل)، رفع وحذف مرفقات (Supabase Storage)، حذف القائمة بالكامل |
| ملفات ومستندات | `src/app/(protected)/files/page.tsx` | رفع (لمن له صلاحية فقط عبر جدول `file_uploaders` أو دور أدمن)، تنزيل (متاح للجميع)، حذف |
| إدارة القاعات | `src/app/(protected)/admin/rooms/page.tsx` | إضافة/حذف قاعة، بحقلين لكل نص (عربي وإنجليزي منفصلين: name/name_en, location/location_en) |
| المستخدمون | `src/app/(protected)/admin/users/page.tsx` | تغيير دور المستخدم (admin/room_manager/coordinator/employee)، تفعيل/إيقاف الحساب |

## 4. مخطط قاعدة البيانات (Supabase، schema: public)
موجود بالكامل في `supabase/migrations/`:
- **0001_initial_schema.sql** — كل الجداول: `profiles` (يمتد من `auth.users`)، `rooms`، `room_managers`، `bookings`، `request_categories`، `requests`، `checklists`، `checklist_items`، `checklist_assignments`، `checklist_access`، `checklist_attachments`، `shared_files`، `app_settings`، `checklist_creators`، `file_uploaders`، `audit_log`
- **0002_row_level_security.sql** — سياسات RLS مفعّلة على كل الجداول (بديل دوال `requireAdmin`/`canEditChecklist` اليدوية في النظام القديم، لكن على مستوى قاعدة البيانات نفسها)

### دوال SQL مساعدة مهمة
- `is_admin()` — هل المستخدم الحالي أدمن؟
- `can_edit_checklist(checklist_id)` — هل يقدر يعدّل قائمة تحقق معيّنة؟
- `can_view_checklist(checklist_id)` — هل يقدر يشوف كل بنودها؟

### Storage Buckets مطلوبة (لازم تتأكدي إنها موجودة)
- `checklist-attachments` (Private)
- `shared-files` (Private)
مع سياسات RLS على `storage.objects` تسمح للمستخدمين المسجّلين (`authenticated`) بالرفع/التنزيل/الحذف.

## 5. دروس مستفادة أثناء البناء (مهم جدًا تتجنبيها)
1. **أي `setAll(cookiesToSet)`** في ملفات Supabase (`server.ts`, `middleware.ts`) لازم يكون ليها نوع TypeScript صريح (`{ name: string; value: string; options?: CookieOptions }[]`)، وإلا Vercel يرفض البناء بخطأ "implicitly has an 'any' type" — حصل مرتين وأخدنا وقت نكتشفه.
2. **قبل تسليم أي كود**، لازم يتفحص فعليًا بـ `npm run build` (مش تخمين) — بنعمل كده بمفاتيح Supabase وهمية في `.env.local` محليًا للتأكد من عدم وجود أخطاء TypeScript/بناء.
3. **الرفع لـ GitHub** بيتم من الـ Terminal على جهاز المستخدم (macOS) بأوامر `git add . && git commit -m "..." && git push` — أحيانًا يحتاج `git push --force` لو فيه تعارض مع تعديلات سابقة من واجهة GitHub مباشرة.
4. **متغيرات البيئة في Vercel**: `NEXT_PUBLIC_SUPABASE_URL` و`NEXT_PUBLIC_SUPABASE_ANON_KEY` — لازم قيمهم تتنسخ **بزر النسخ المباشر** من `Supabase → Settings → API`، مش تكتب يدويًا (حصلت مشكلة قيمة فاضية بالغلط).
5. بعد أي تعديل في متغيرات البيئة، لازم **Redeploy** يدوي من Vercel (بدون "Use existing Build Cache") عشان القيم الجديدة تتطبّق.

## 6. المطلوب في المحادثة الجديدة
[اكتبي هنا بالتفصيل: هل في مشكلة معيّنة؟ ميزة جديدة؟ صفحة محتاجة تحسين؟]
